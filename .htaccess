Options -Indexes
Options +FollowSymLinks

#RewriteEngine On
    RewriteBase /~cg236/Capstone/

RewriteEngine on
RewriteCond $1 !^(index\.php|images|assets|robots\.txt)
RewriteRule ^(.*)$ index.php/$1 [L]
# Removes trailing slashes (prevents SEO duplicate content issues)
